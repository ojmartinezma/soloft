<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
    </style>
    <style name="logo_main" parent="Theme.AppCompat.Light.DarkActionBar">

    </style>
    <style name="logo_secundario" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="android:src">@drawable/logo</item>
        <item name="android:contentDescription">Logo</item>
        <item name="android:paddingLeft">@dimen/logo_secundario_pad_left</item>
        <item name="android:paddingBottom">@dimen/logo_secundario_pad_bottom</item>
        <item name="android:scaleType">fitStart</item>

    </style>
</resources>
